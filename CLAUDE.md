# CLAUDE.md

이 파일은 Claude Code가 본 프로젝트에서 작업할 때 참고하는 프로젝트 컨텍스트 문서입니다.
모든 설명·커밋 메시지·코드 주석은 한국어를 기본으로 합니다.

---

## 1. 프로젝트 개요

- **목적**: 사용자의 할 일(Task)과 일정을 효율적으로 관리하는 생산성 애플리케이션.
- **출시 전략**: **Windows 10/11 데스크톱 앱을 우선 개발** → 이후 동일 데이터 연동으로 **Android 앱으로 확장**.
- **개발 방법론**: 바이브 코딩 (자연어 지시 → AI가 코드 작성). 작업은 작게 쪼개고, 한 단계씩 화면으로 확인하며 진행한다.

---

## 2. 기술 스택

| 구분 | 내용 | 상태 |
| --- | --- | --- |
| 프레임워크 | **Flutter** (단일 코드베이스) | 확정 |
| 언어 | **Dart** | 확정 |
| 1차 타깃 | **Windows 데스크톱** | 확정 |
| 2차 타깃 | Android | 추후 |
| IDE / AI | VS Code + Claude Code | 확정 |
| 데이터베이스 | **Firebase Firestore** (프로젝트: `todo-app-c940e`, 익명 인증으로 설치별 UID 발급 후 `users/{uid}/tasks`, `users/{uid}/categories`에 저장) | 확정 |
| 상태 관리 | **setState** (Provider/Riverpod 등 미도입) | 확정 |

> 데이터베이스는 Firebase Firestore, 상태 관리는 `setState`로 확정되었다(2026-07-06 Firebase 실연동 및 검증 완료). 이제 이 항목들을 다시 사용자에게 제안할 필요는 없다.

---

## 3. 개발 원칙

- **데스크톱 우선**: 모든 화면·기능은 먼저 Windows 데스크톱에서 동작하도록 구현한다. 모바일 전용 분기는 추후.
- **UI 먼저, 로직 나중**: 레이아웃과 더미 데이터로 화면 골격을 먼저 잡고, 그 다음에 상태/데이터 연동을 붙인다.
- **작은 단위로 진행**: 한 번에 한 영역(예: 좌측 TASK 바)만 만들고 Hot Reload로 확인한다.
- **불확실하면 멈추고 질문**: 기획서에 없는 사항(예: 색상, 정확한 동작, DB 스키마)을 임의로 단정하지 말고 사용자에게 확인한다.

---

## 4. 화면 구조 명세 (기획서 기준)

전체 화면은 **3단 패널 구조**: `좌측 TASK 바 — 중앙 메인 화면 — 우측 상세 설정 화면`.

### 4.1 좌측 TASK 바
- **기본 카테고리 4개 (상단부터 순서 고정)**: `오늘 할일` → `계획된 일정` → `계획 안된 일정` → `모두`
- 기본 카테고리 아래 **얇은 구분선**, 그 아래에 **사용자 지정 카테고리 목록** 표시.
  - 사용자 지정 카테고리 항목은 이름 왼쪽에 **색상 점(dot)** 을 표시하며, 클릭하면 색상 선택 다이얼로그가 열려 언제든 색을 바꿀 수 있다(팔레트 8색, `lib/models/category.dart`의 `categoryColorPalette`). 이 색은 달력 뷰의 일정 바/블록 색상으로도 그대로 쓰인다.
  - 항목 오른쪽 **×(삭제) 아이콘**으로 삭제 가능. 확인 다이얼로그 후 삭제되며, 그 카테고리에 속한 할 일은 삭제되지 않고 **'카테고리 없음'**으로 이동한다(categoryId를 null로 변경).
- **하단 버튼 (아래에서 위 순서)**:
  1. **새 카테고리 생성 버튼** — TASK 바 가장 하단. 클릭 시 이름 입력 + 색상 선택(8색 팔레트) 다이얼로그가 열린다.
  2. **달력 전환 버튼** — 새 카테고리 버튼 바로 위. 클릭 시 중앙 메인을 달력 뷰로 전환.
- **다크/라이트 모드 전환 버튼은 없다** — 앱은 항상 다크 모드로 고정 실행된다(사용자 요청으로 토글 버튼 제거, `lib/main.dart`의 `themeMode: ThemeMode.dark` 고정).

### 4.2 중앙 메인 화면
- **리스트 뷰**: 선택된 카테고리의 할 일 목록. 각 할 일 **좌측 동그라미 버튼**을 누르면 완료(Checked) 처리. 항목 우측에 **×(삭제) 아이콘**이 있다.
- 목록 **하단**에 **할 일 추가 입력행**이 있어(기획서엔 없던 항목), 제목을 입력하고 Enter(또는 + 버튼)로 즉시 추가한다. 현재 선택된 카테고리 컨텍스트에 맞춰 `isToday`/`categoryId`가 채워지고, 기한·반복 등 나머지는 상세 패널에서 추가로 설정한다. 배경색을 목록과 다르게(`surfaceAlt` + 위쪽 구분선) 주어 전체 배경과 구분되는 입력 바처럼 보이게 한다.
  - **기본 4개 뷰(오늘 할일 등)에서만** 입력행에 **카테고리 선택 토글(팝업 메뉴)** 이 함께 있어, 추가할 할 일을 특정 사용자 지정 카테고리로 바로 지정할 수 있다(기본값 '카테고리 없음'). 특정 카테고리 뷰(`TaskListView`)에서는 그 카테고리로 항상 고정되므로 이 토글이 없다.
- **기본 4개 뷰(오늘 할일/계획된 일정/계획 안된 일정/모두) 전부** 사용자 지정 카테고리별로 그룹화하여 표시한다 (기획서 원문은 '계획 안된 일정'에만 그룹화라고 되어 있었으나, 5단계 구현에서 일관성을 위해 4개 뷰 전체로 통일했다 — 실제 동작 기준으로 갱신).
- **오늘 할일 뷰 전용 — 작업 제안 버튼**: 헤더 우측(완료 항목 숨기기 버튼 왼쪽)에 전구 아이콘 버튼이 있어(기획서엔 없던 항목), 누르면 아직 '오늘 할일'에 없는 모든 작업 목록이 뜨고, 각 항목의 + 버튼을 누르는 즉시(별도 확인 없이) 그 작업이 '오늘 할일'로 추가되고 목록에서 사라진다.
- **달력 뷰** (달력 전환 버튼 클릭 시):
  - **기간 일정**(시작일+종료일): 해당 기간만큼 이어지는 **긴 바(Bar)** 형태.
  - **단일 일정**(마감 기한만): 해당 날짜 칸에 **하루 블록** 형태.
  - 일정의 색상은 소속 카테고리에 사용자가 지정한 색상을 그대로 사용한다(카테고리 없음이면 회색).

### 4.3 우측 상세 설정 화면
특정 할 일 클릭 시 우측 끝에 **슬라이드/팝업**으로 등장. 위→아래 순서:
1. **'오늘 할일'에 추가** 버튼
2. **기한 설정** — 시작일–종료일 구간 지정 또는 단일 마감 기한
3. **반복 설정** — 사용자 지정(며칠마다) / 매일 / 매주 / 매월 / 매년
4. **알림 설정** — **화면 UI로만 배치** (백엔드 로직 없음, 아래 6번 참고)
5. **메모** 입력란

---

## 5. 데이터 모델 (확정, Firestore 스키마 기준)

- **Task(할 일)** — `lib/models/task.dart`: `id`, `title`, `isCompleted`, `categoryId`(null 가능), `isToday`, `startDate`/`dueDate`(둘 다 null이면 계획 안된 일정), `repeatType`(none/daily/weekly/monthly/yearly/custom), `repeatIntervalDays`, `reminderEnabled`(알림 UI 값, 백엔드 로직 없음), `memo`.
- **Category(카테고리)** — `lib/models/category.dart`: `id`, `name`, `type`(today/planned/unplanned/all/custom), `color`(사용자 지정 색상, ARGB int로 저장). 기본 4개 뷰(오늘 할일·계획된 일정·계획 안된 일정·모두)는 **저장 대상이 아니라 필터/뷰**로 취급하며 `defaultCategories` 상수로 고정되어 있다.
- Firestore 경로: `users/{uid}/tasks/{taskId}`, `users/{uid}/categories/{categoryId}` (익명 인증 UID 기준, `lib/services/firestore_service.dart`). 카테고리 문서에는 정렬용 `order` 필드가 추가로 저장된다.

---

## 6. 범위 경계 — 지금은 하지 않는 것

다음은 **현 단계에서 구현하지 않는다** (지시받아도 확인 후 진행):

- **알림 백엔드 로직**: 알림 설정은 **UI 배치까지만**. 기한·반복 시각에 맞춘 OS 푸시 알림 등 실제 알림 발송 기능은 **추후 단계**.
- **모바일(Android) 전용 UI 기획**: 별도 문서로 추후 진행. 지금은 데스크톱 레이아웃에 집중.

---

## 7. 개발 명령어

```bash
flutter run -d windows     # Windows 데스크톱으로 실행
flutter devices            # 실행 가능 대상 확인
flutter doctor -v          # 환경 점검
flutter analyze            # 정적 분석
flutter test               # 테스트 실행
```

---

## 8. 권장 프로젝트 구조 (추천 — 강제 아님)

```
lib/
  main.dart
  models/        # Task, Category 등 데이터 모델
  screens/       # 리스트 뷰, 달력 뷰 등 화면
  widgets/       # TASK 바, 상세 패널 등 재사용 위젯
  services/      # (추후) 데이터/BaaS 연동
```

---

## 9. 코드 컨벤션

- Dart 공식 스타일(`flutter analyze` 통과)을 따른다.
- 위젯/화면은 기능 단위로 파일을 분리한다.
- 주석·문자열·문서는 한국어 기본.
- 큰 변경 전에는 무엇을·왜 바꾸는지 먼저 짧게 설명한다.

---

## 10. 결정 완료 내역

| 항목 | 결정 | 비고 |
| --- | --- | --- |
| 데이터베이스 | Firebase Firestore (`todo-app-c940e`) | 2단계~6단계에서 구조 구축 후 실연동 완료·검증 완료 |
| 상태 관리 | setState | Provider/Riverpod 등 미도입, 앞으로도 필요 전까지 유지 |
| 데이터 모델 스키마 | 5번 항목 확정 스키마 | Task/Category 필드 확정 (Category `color` 포함) |

미확정 항목은 현재 없다. 새로운 미확정 사항이 생기면 본 문서를 갱신한다.

## 11. 현재까지 구현 상태 (요약)

- **1~6단계 (기획서 범위)**: 3단 패널 레이아웃, 좌측 TASK 바, 중앙 리스트/그룹화 뷰, 달력 뷰, 우측 상세 설정 패널(오늘 할일/기한/반복/알림 UI/메모), 데이터 모델 확정, Firebase Firestore 실연동(익명 인증 + 보안 규칙 배포)까지 모두 완료.
- **7단계 이후 (기획서엔 없었지만 실사용을 위해 추가한 기능)**:
  - 할 일 추가(리스트 하단 입력행, 배경색으로 구분 + 기본 뷰에서는 카테고리 선택 토글 포함), 할 일/카테고리 삭제(× 아이콘 + 카테고리는 확인 다이얼로그).
  - 카테고리별 색상 지정/변경(생성 시 선택 + 색상 점 클릭으로 언제든 변경), 달력 뷰가 이 색을 그대로 사용.
  - 다크 모드 고정 실행(토글 버튼은 사용자 요청으로 제거, 라이트 모드 없음).
  - '오늘 할일' 뷰의 작업 제안 버튼 — 아직 오늘 할일이 아닌 작업들을 모아 보여주고, + 버튼으로 즉시 추가.
- **여전히 미구현 (§6 범위 경계, 의도적으로 보류)**: 알림 백엔드 로직(실제 푸시 발송), Android 전용 UI.
- **알려진 잔여 이슈**: `test/widget_test.dart`가 기본 Flutter 카운터 앱 템플릿 그대로 남아 있어 `flutter test` 실행 시 실패한다(아직 갱신 안 함).
